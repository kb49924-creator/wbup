# ============================================
# Wildberries Channel Manager — зависимости
# ============================================
# Установка: pip install -r requirements.txt
# ============================================

# --- HTTP клиент (WB API, загрузка фото) ---
requests>=2.31.0

# --- Асинхронный HTTP клиент ---
aiohttp>=3.9.0

# --- Браузерный движок для обхода антибота WB ---
playwright>=1.40.0

# --- Обработка изображений + NumPy для быстрого скоринга фото ---
Pillow>=10.0.0
numpy>=1.26.0

# --- Удаление фона с фото (ИИ) ---
rembg[cpu]>=2.0.0

# --- Web GUI Server (FastAPI + Uvicorn) ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# --- Telegram Bot API ---
python-telegram-bot>=20.0

# --- Оптимизация для macOS / Linux ---
uvloop>=0.19.0; sys_platform != 'win32'